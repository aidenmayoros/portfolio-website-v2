import { Button, Box } from '@mui/material';

function Projects() {
	return (
		<Box id='projects'>
			<h1>Projects Page</h1>
			<Button onClick={() => true}>Back</Button>
		</Box>
	);
}

export default Projects;
